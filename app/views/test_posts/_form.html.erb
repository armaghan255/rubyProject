<%= form_with(model: test_post, local: true) do |form| %>
  <% if test_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(test_post.errors.count, "error") %> prohibited this test_post from being saved:</h2>

      <ul>
        <% test_post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :desc %>
    <%= form.text_area :desc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
